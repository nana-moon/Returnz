package bunsan.returnz.persist.repository;

import org.springframework.data.jpa.repository.JpaRepository;

public interface DotcomThemeCompanyRepository extends JpaRepository<DotcomThemeCompanyRepository, String> {
}

import React from 'react';
import tw, { styled } from 'twin.macro';

export default function InfoResult() {
  return <InfoContainer>InfoResult</InfoContainer>;
}

const InfoContainer = styled.div`
  ${tw`border-2 border-black bg-white`}
`;
